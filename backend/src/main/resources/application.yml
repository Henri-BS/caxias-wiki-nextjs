spring:
  datasource:
    url: jdbc:postgresql://localhost:8080/caxiasdestiny
    username: postgres
    password: postgres
  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
    show-sql: true
