'use client'

import { Template } from "@/components/Template";

export default function NotFoundPage(){
    return(
        <Template>
            <section>
                <h1 className="text-gray-900 text-x1 text-center">
                    Página não encontrada!
                </h1>
            </section>
        </Template>
    );
}